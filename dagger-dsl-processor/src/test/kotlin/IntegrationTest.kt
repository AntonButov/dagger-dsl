
import io.kotest.core.spec.style.StringSpec

class IntegrationTest : StringSpec({

    "simple file dsl should to compile, then load and call method should return data dsl object" {

        //  result.componentClass.name shouldBe "CoffeeShop"
        // TODO: Reimplementing test for PSI-based analysis instead of dynamic compilation
        // Use https://github.com/AntonButov/gradle-tester
    }
})
